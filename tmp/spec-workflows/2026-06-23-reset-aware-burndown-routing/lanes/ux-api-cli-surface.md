# Lane: ux-api-cli-surface

Status: partially accepted candidate evidence

Accepted product constraints:

- Default quota status should be account-centric.
- Human output should show 5h left, weekly left, reset timing, pace/runout, and routing decision.
- Avoid `pp` and avoid `bottleneck` in default user-facing language.
- Use `left`, `limiting window`, `weekly pressure`, `selected next`, `held`, `blocked`, and `needs refresh`.
- Keep default output to no more than two rendered lines per account.

Parent note:

The lane could not inspect local Rust files, so repo-fit claims were parent-verified separately before acceptance.
